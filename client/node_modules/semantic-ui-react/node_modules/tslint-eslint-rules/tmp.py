import re

regex = r"(?P<msg>.*?) \((?P<hash>.*?)\)"
a = open('CHANGELOG.md', 'r')

for line in a:
    m = re.match(regex, line)
    if m:
        h = m.group('hash')
        print '%s ([%s](https://github.com/buzinas/tslint-eslint-rules/commit/%s))' % (m.group('msg'), h[:7], h)
    else:
        print line[:-1]

